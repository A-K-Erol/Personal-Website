# Personal Website
Originally, developed using an XAMPP (Apache with MariaDB, PhP, and Perl) server and WordPress libraries. Exported as a static html site with the Simply Static plugin. Hosted on GitHub pages with a custom domain name. Now, it is maintained up to date with an automatic updater that scans my resume.
